<div id="listing-section" class="py-5">
    <div class="container text-center">
        <div class="row">
            <div class="col">
                <h4>Latest Listings</h4>
            </div>
        </div>
        <div class="card-deck">
            {% for listing in listings %}
                {% include "listing.jinja" %}
            {% endfor %}
        </div>
    </div>
</div>